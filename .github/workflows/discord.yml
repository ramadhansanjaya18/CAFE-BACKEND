name: Discord Notification

on:
  push:
    branches:
      - main
      - develop
      - feature/**

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Send Discord Notification
        run: |
          curl -H "Content-Type: application/json" \
          -X POST \
          -d "{\"content\":\"🚀 New push detected in CAFE-BACKEND repository\"}" \
          ${{ secrets.DISCORD_WEBHOOK }}
